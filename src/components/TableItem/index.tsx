import { Container } from './styles'
import { Student } from '../../types/Student'

type Props = {
    key: number;
    person: Student;
}

export const TableItem = ({key, person}: Props) => {
    return (
        <Container>
            
        </Container>    
    )
}